  - name: "INSTALL SUDO AND ADD {{ user_name }} TO SUODERS"
    shell: >
      ssh root@{{ host_ip }} 'apt update &&  
      apt-get install sudo && 
      echo "{{ user_name }} ALL=(ALL) NOPASSWD: ALL" > /etc/sudoers.d/{{ user_name }}'